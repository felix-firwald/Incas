<UserControl x:Class="Incubator_2.Forms.TableFiller"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Incubator_2.Forms"
             mc:Ignorable="d" 
             d:DesignHeight="160" d:DesignWidth="800">
    <Grid MinHeight="80">
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <Button Style="{DynamicResource ButtonMain}" MinWidth="200" Content="Вставить из базы данных" Click="InsertClick"/>
        </StackPanel>
        <DataGrid MinHeight="160" Style="{DynamicResource DataGridMain}" ItemsSource="{Binding Grid}" Grid.Row="1"/>
    </Grid>

</UserControl>
