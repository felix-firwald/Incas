<UserControl x:Class="Incas.Objects.Views.Pages.CustomDatabaseMain"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Incas.Objects.Views.Pages" xmlns:fa="http://schemas.fontawesome.io/icons/"
             mc:Ignorable="d" 
             d:DesignHeight="650" d:DesignWidth="1200" Cursor="">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="13*"/>
            <ColumnDefinition Width="35*"/>
        </Grid.ColumnDefinitions>
        <GroupBox Style="{StaticResource MainGroupBox}" Header="ВЫБОР" Margin="5,5,5,5">
            <ScrollViewer VerticalScrollBarVisibility="Auto" Grid.RowSpan="3">
                <StackPanel >
                    <Expander Style="{DynamicResource ExpanderMain}" Header="Категории" IsExpanded="True">
                        <StackPanel>
                            <ListBox 
                    Style="{DynamicResource ListBoxMain}"
                    ItemContainerStyle="{DynamicResource ListBoxItemPurple}"
                    ItemsSource="{Binding Categories}" SelectedItem="{Binding SelectedCategory}" Background="{x:Null}" Grid.RowSpan="2" Margin="0,-2,0,-2" ScrollViewer.VerticalScrollBarVisibility="Auto">
                            </ListBox>
                        </StackPanel>
                    </Expander>
                    <Expander Style="{DynamicResource ExpanderMain}" Header="Классы объектов" IsExpanded="True" Visibility="{Binding TablesVisibility}">
                        <StackPanel>
                            <ListBox
                    Style="{DynamicResource ListBoxMain}"
                    ItemContainerStyle="{DynamicResource ListBoxItemMicro}"
                    ItemsSource="{Binding Classes}" SelectedItem="{Binding SelectedClass}" DisplayMemberPath="name" Background="{x:Null}" Grid.RowSpan="2" Margin="0,-2,0,-2" ScrollViewer.VerticalScrollBarVisibility="Auto">
                            </ListBox>
                        </StackPanel>
                    </Expander>
                </StackPanel>
            </ScrollViewer>
        </GroupBox>
        <GroupBox x:Name="ContentPanel" Style="{StaticResource MainGroupBox}" Grid.Column="1" Header="{Binding SelectedClassName}" Margin="5,5,5,5"/>
    </Grid>
</UserControl>
